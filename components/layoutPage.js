import styles from './css/layout.module.css';

export default function Layout({ children }) {
  return (<div className={styles.container + " sellashlomy@gmail.com" } >Layout Page 654{children}</div>);
}